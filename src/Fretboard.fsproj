<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
  </PropertyGroup>
  <ItemGroup>
    <!-- Global to the app -->
    <Compile Include="Global.fs" />
    <!-- Info -->
    <Compile Include="Info/View.fs" />
    <!-- Theory -->
    <Compile Include="Theory/Scale.fs" />
    <Compile Include="Theory/Tab.fs" />
    <!-- Scale -->
    <Compile Include="Scale/Types.fs" />
    <Compile Include="Scale/State.fs" />
    <Compile Include="Scale/View.fs" />
    <!-- Chord -->
    <Compile Include="Chord/Types.fs" />
    <Compile Include="Chord/State.fs" />
    <Compile Include="Chord/View.fs" />
    <!-- Navbar -->
    <Compile Include="Navbar/View.fs" />
    <!-- App -->
    <Compile Include="Types.fs" />
    <Compile Include="State.fs" />
    <Compile Include="App.fs" />
  </ItemGroup>
  <Import Project="..\.paket\Paket.Restore.targets" />
</Project>
